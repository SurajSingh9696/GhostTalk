# MongoDB Connection String
# Get this from MongoDB Atlas: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ghosttalk?retryWrites=true&w=majority

# JWT Secret Key
# Generate a random string (use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your_random_secret_key_minimum_32_characters_long

# Resend API Key
# Get this from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Application URL (REQUIRED for emails and OAuth)
# For local development: http://localhost:3000
# For production: https://your-app-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Socket.IO Server URL (OPTIONAL - only needed for custom server deployments)
# Leave empty or omit for Vercel/serverless deployments (will use HTTP fallback)
# For Render or VPS with custom server: https://your-app-domain.com
# For local development: http://localhost:3000
NEXT_PUBLIC_SOCKET_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
